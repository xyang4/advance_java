<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xyang.mybatis.mapper.MsgMapper">
	<resultMap type="com.xyang.mybatis.entity.Message" id="MessageResult">
		<id column="ID" jdbcType="INTEGER" property="id" />
		<result column="COMMAND" jdbcType="VARCHAR" property="command" />
		<result column="DESCRIPTION" jdbcType="VARCHAR" property="description" />
		<result column="CONTENT" jdbcType="VARCHAR" property="content" />
	</resultMap>

	<select id="queryMessageList" parameterType="java.util.Map" resultMap="MessageResult">
		select
		<include refid="columns" />
		from message
		<where>
			<if test="message.command != null and ''!=message.command.trim()">
				and command=#{message.command}
			</if>
			<if test="message.description != null">
				and description like '%' #{message.description} '%'
			</if>
		</where>
		order by id limit #{page.dbIndex},#{page.dbNumber}
	</select>
	<select id="queryMessageListByPage" parameterType="java.util.Map"
		resultMap="MessageResult">
		select
		<include refid="columns" />
		from MESSAGE
		<where>
			<if test="message.command != null">
				and COMMAND=#{message.command}
			</if>
			<if test="message.description != null">
				and DESCRIPTION like '%' #{message.description} '%'
			</if>
		</where>
		order by ID
	</select>
	<select id="count" parameterType="com.xyang.mybatis.entity.Message" resultType="int">
		select count(*) from MESSAGE
		<where>
			<if test="command != null">
				and COMMAND=#{command}
			</if>
			<if test="description != null">
				and DESCRIPTION like '%' #{description} '%'
			</if>
		</where>
	</select>
	<sql id="columns">ID,COMMAND,DESCRIPTION,CONTENT</sql>
</mapper>